import Root from './config.svelte';
import Input from './config-field_errors.svelte';
import Button from './config-submit.svelte';

export default Root;
export {
  Input,
  Button,
  //
  Root as Config,
  Input as ConfigInput,
  Button as ConfigButton,
};
